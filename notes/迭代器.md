```python
# DataLoader的实例，是一个可迭代对象
loader = DataLoader(dataset, batch_size=32)

# 方式一：分步写
iterator = iter(loader)    # 1. 获取迭代器
batch = next(iterator)     # 2. 取一个 batch

# 方式二：合并写（常见）
batch = next(iter(loader))

# 方式三：for 循环（自动处理 iter 和 next）
for batch in loader:
    ...
```